# 0.7.x API Freeze Checklist

This checklist operationalizes the `0.7.x` stage in `devguide/ROADMAP.md`.
Its goal is to freeze ArgDigest public contracts before entering `0.9.x` RC.

## Status Legend

- `[x]` complete and verified
- `[ ]` pending
- `[-]` intentionally deferred

## A) Public API surface freeze

- [x] lock exported symbols in `argdigest.__all__`.
- [x] lock public constructor/signature behavior for:
  - `arg_digest`,
  - `arg_digest.map`,
  - `argument_digest`,
  - `register_pipeline`,
  - `get_pipelines`.
- [x] document compatibility policy for `DigestConfig` fields.
- [x] verify no unintended public import paths are relied on in docs/examples.

## B) Contract tests

- [x] add tests that assert public symbols exist and remain importable.
- [x] add signature snapshot tests for key public callables.
- [x] add behavior-lock tests for strictness modes and config precedence.
- [x] add regression tests for wheel install/import behavior.

## C) Deprecation policy

- [x] identify any legacy aliases/behaviors that should be deprecated before `1.0.0` (none planned).
- [x] define timeline and message policy for deprecations (if any).
- [x] ensure deprecation diagnostics are catalog-based and actionable.

## D) Documentation freeze alignment

- [x] ensure all user-facing examples use only frozen API.
- [x] ensure developer docs reflect the exact public contract.
- [ ] ensure API reference pages match exported symbols.

## E) Release gates for 0.7.x closure

- [ ] `pytest` green in CI.
- [ ] `ruff check .` green in CI.
- [ ] docs build and publish pipeline green.
- [ ] no planned breaking API changes remain before `1.0.0`.

## Exit Decision

- [ ] mark `0.7.x` complete and open `0.9.x` RC checklist.
