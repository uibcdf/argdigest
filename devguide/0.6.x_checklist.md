# 0.6.x Integration Hardening Checklist

This checklist operationalizes the `0.6.x` stage defined in `devguide/ROADMAP.md`.
Use it as the release gate before opening the `0.7.x` API freeze stage.

## Scope

- Argument-centric digestion must be stable in `package`, `registry`, `decorator`, and mixed styles.
- Optional integrations must behave consistently.
- Docs and runtime behavior must match for core `arg_digest` flows.

## Status Legend

- `[x]` complete and verified
- `[ ]` pending
- `[-]` intentionally deferred to next stage

## Objectives

### A) Multi-style digestion reliability

- [x] `package` style covered by smoke examples (`examples/packlib`) and tests.
- [x] `registry` style covered by smoke examples (`examples/reglib`) and tests.
- [x] `decorator` style covered by unit tests (`@argument_digest`).
- [x] mixed mode (digesters + pipelines) covered by tests.

### B) Optional integrations

- [x] beartype integration tested.
- [x] pydantic integration tested.
- [x] pyunitwizard integration tested.
- [x] depdigest-based optional dependency routing is active in core decorator.

### C) Config behavior and defaults

- [x] explicit `config="<pkg>._argdigest"` behavior tested.
- [x] auto-discovery of `<root_package>._argdigest` tested via examples.
- [x] pipeline-only mode without configured digesters no longer emits bulk non-actionable warnings.

### D) Error/warning semantics

- [x] catalog-backed exception/warning messages are active.
- [x] strictness modes (`warn`, `error`, `ignore`) covered by tests.
- [x] legacy warning policy documented for compatibility test (`syntax` warning is accepted baseline in `0.6.x`).

### E) Documentation consistency

- [x] user guide includes simple and dependent digester templates.
- [x] devguide moved and refreshed with roadmap/index.
- [x] AI agent templates cover package/registry/mixed/decorator styles.
- [x] final pass completed; user/developer docs and README aligned with current API.

### F) Quality gates

- [x] full test suite passes locally (`84 passed`).
- [x] lint baseline passes (`ruff check .`).
- [x] validate docs build in docs environment as `0.6.x` close condition.
- [x] ensure CI/docs import smoke checks fail hard if package import fails.

## Exit Criteria Mapping (from Roadmap)

### 1) Full test suite passes in a clean environment

- Current status: **PASS** (`pytest` green locally and CI green remotely).
- Close condition: satisfied.

### 2) No unresolved docs/runtime mismatches for `arg_digest`

- Current status: **PASS**.
- Close condition: satisfied.

### 3) Smoke examples validated in CI

- Current status: **PASS** (smoke tests are part of the mandatory pytest suite in CI).
- Close condition: satisfied.

## 0.6.x Closure Decision

`0.6.x` is closed.

Next step: execute `0.7.x` API freeze checklist.
