# ArgDigest 0.9.2 Release Notes (Draft)

## Release Position

`0.9.2` is the latest pre-1.0 stabilization checkpoint.
It closes the `0.9.x` operational window while keeping the `1.0.0` tag as an
explicit owner decision.

## What 0.9.2 consolidates

- full `0.9.x` checklist closure and evidence sync,
- `1.0.0` checklist closure and final release/migration narrative alignment,
- go/no-go handoff pack for final owner approval:
  - `devguide/1.0.0_go_no_go_pack.md`.

## Validation snapshot

- local tests: `pytest -q` green.
- local docs build: `make -C docs html` green.
- ecosystem health: `argdigest health-check` all checks PASS.

## Compatibility notes

- no public API break relative to `0.9.1`,
- no planned feature intake before `1.0.0`,
- this tag is intended as a stabilization checkpoint, not a contract expansion.
