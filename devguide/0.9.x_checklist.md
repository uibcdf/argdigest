# 0.9.x RC Consolidation Checklist

`0.9.x` is the final release-candidate consolidation window before `1.0.0`.
It must contain the full intended `1.0.0` behavior, then remain under real
usage for a short stabilization period.

## Status Legend

- `[x]` complete and verified
- `[ ]` pending
- `[-]` intentionally deferred

## A) Functional parity with 1.0 target

- [x] sections A, B, C, D, and F from `devguide/1.0.0_checklist.md` are fully green.
- [x] section E release gates are green on remote workflows for the `0.9.0` tag.
- [x] no planned feature additions after `0.9.0` (bugfixes and docs only).

## B) Release-candidate operations

- [x] tag and publish `0.9.0`.
- [x] CI workflow green on `0.9.0`.
- [x] docs workflow green and gh-pages updated from `0.9.0`.
- [x] conda build/upload workflow green on `0.9.0`.
- [x] wheel install/import checks green on `0.9.0`.

## C) Consolidation window (real usage)

- [x] define consolidation period start/end dates.
- [x] use `0.9.0` in daily integration scenarios (MolSysMT target paths included).
- [x] collect regressions, warnings, and friction points.
- [x] shared collective E2E module established and running locally (`tests/e2e/test_collective_error_path.py`).
- [x] release `0.9.x` patch fixes if needed (`0.9.1`, `0.9.2`, ...).
- [x] close window with zero blocker issues open.

Consolidation window:
- start: 2026-03-03
- target end: 2026-03-10

Remote validation note (2026-03-03):
- tag `0.9.0`: CI green, wheel install/import green, docs build + gh-pages green,
  conda build/upload green.

Checkpoint note (2026-03-04):
- sibling status observed from this workspace:
  - `argdigest`: `0.9.0-9-gc543c1a`
  - `depdigest`: `0.10.0-1-gcb93e7f`
  - `smonitor`: `0.11.4-16-ge0e1a8c`
  - `pyunitwizard`: `0.21.1-1-g9fd9b46`
- collective E2E artifact reference: `../molsyssuite/devguide/collective_e2e_artifact_2026-03-04.md`.
- remaining C-items stay intentionally open until consolidation window close and final blocker review.
- MolSysMT-like representative path coverage added: `tests/test_molsysmt_representative_paths.py`.
- recorded regressions/friction in this window:
  - docs workflow import failure due to missing packaged `argdigest._private` (fixed by package discovery update),
  - CI syntax fixes in workflow files (fixed and green reruns),
- no open blocker regressions currently observed in ArgDigest core behavior.
- open GitHub issues check (2026-03-04): `gh issue list --state open --limit 200` -> `[]`.
- latest CI run on head `5db1e5c` is green (`gh run list`, run `22659898739`, workflow `CI`).

## D) Exit criteria to 1.0.0

- [x] no API-breaking changes since `0.9.0`.
- [x] no blocker bugs in digestion/config/diagnostics/pipeline paths.
- [x] `devguide/1.0.0_checklist.md` fully green.
- [x] final `1.0.0` notes and migration summary prepared.
