channels:
  - uibcdf
  - conda-forge

dependencies:
  # Core tools for building and uploading conda packages
  - conda-build
  - anaconda-client

  # Python packaging helpers
  - pip
  - setuptools >=61.0
  - versioningit >=3.0
  - python-build
