channels:
  - uibcdf
  - conda-forge

dependencies:
  # --- Python base ---
  - python
  - pip

  # --- Test tools ---
  - pytest
  - pytest-receptor
  - pytest-cov
  - pytest-xdist
  - ruff

  # --- Dependencies ---
  - beartype
  - pydantic
  - pyunitwizard
  - versioningit
  - pyyaml
  - numpy
  - pandas
  - smonitor
  - depdigest
